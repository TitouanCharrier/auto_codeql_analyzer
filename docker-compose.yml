version: '3.9'

services:
#  metabase:
#    image: metabase/metabase:latest
#    container_name: metabase_sqlite
#    ports:
#      - "3000:3000"
#    volumes:
#      - ./data:/metabase-data
#    environment:
#      - MB_DB_FILE=/metabase-data/metabase.db
#    restart: always

  codeql:
    build: .
    container_name: AutoCodeQlAnalyze
    stdin_open: true
    tty: true
    volumes:
      - ./data:/workspace/data
    working_dir: /workspace


